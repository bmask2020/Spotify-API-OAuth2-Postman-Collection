# Spotify-API-OAuth2-Postman-Collection

## 💡 نظرة عامة على المشروع

هذه مجموعة Postman (Collection) مصممة لاختبار وتوثيق تدفق المصادقة **OAuth 2.0 Authorization Code Flow** وتفاعلات إدارة قوائم التشغيل الخاصة بـ **Spotify Web API**.

تبرز هذه المجموعة مهارات متقدمة في اختبار الـ APIs، واستخدام المتغيرات البيئية (Environment Variables)، وتضمين نصوص اختبار (Test Scripts) لضمان جودة الاستجابة.

## ✨ الوظائف التي يتم اختبارها

تغطي هذه المجموعة العمليات التالية:

1.  **المصادقة:**
    * الحصول على رمز التفويض (Authorization Code).
    * تبديل رمز التفويض برمز الوصول (Access Token).
2.  **معلومات المستخدم:**
    * الحصول على بيانات المستخدم الحالي (`Get User Id`).
3.  **إدارة قوائم التشغيل (Playlist Management):**
    * قراءة قوائم التشغيل الخاصة بالمستخدم (`Get Playlists`).
    * إنشاء قائمة تشغيل جديدة (`Create Playlist`).
    * تحديث قائمة تشغيل موجودة (`Update Playlist`).

## 🛠️ المتطلبات الأساسية

* **برنامج Postman** مُثبت.
* **حساب Spotify للمطورين** (Spotify Developer Account).
* **تطبيق مسجل** على لوحة تحكم مطوري Spotify للحصول على:
    * `Client ID`
    * `Client Secret`
    * `Redirect URI` (يجب أن تكون مطابقة للقيمة المستخدمة في ملف البيئة).

## 🚀 كيفية الإعداد والاستخدام

لاستخدام هذه المجموعة، اتبع الخطوات التالية بالترتيب:

### 1. استيراد الملفات

1.  قم بتنزيل الملفين التاليين من هذا المستودع:
    * `Spotify-API-OAuth2-Postman-Collection.json`
    * `Spotify API Credentials.json` (ملف البيئة)
2.  في Postman، قم باستيراد ملف **Collection** أولاً، ثم استورد ملف **Environment**.
3.  في الزاوية العلوية اليمنى من Postman، تأكد من تحديد بيئة **`Spotify API Credentials`**.

### 2. تحديث المتغيرات البيئية (🔑 الأمان أولاً)

انتقل إلى إدارة البيئات (Manage Environments) وقم بتحديث المتغيرات في العمود **CURRENT VALUE**:

| المفتاح (Key) | الوصف | ملاحظة مهمة |
| :--- | :--- | :--- |
| `spotify_client_id` | معرف العميل الخاص بتطبيقك. | مطلوب |
| `spotify_client_secret` | المفتاح السري لتطبيقك. | **لا تشارك هذه القيمة أبدًا علنًا!** |
| `spotify_redirect_uri` | URI لإعادة التوجيه المسجلة في تطبيق Spotify. | مطلوب |

**ملاحظة:** المتغيرات الأخرى مثل `spotify_access_token` و `spotify_authorization_code` سيتم تعيينها تلقائيًا أثناء التدفق.

### 3. خطوات تنفيذ المصادقة والاختبار

لتشغيل التدفق بشكل صحيح، يجب أن تتبع الطلبات بالترتيب:

1.  **Get Access Grant:**
    * قم بتشغيل هذا الطلب. سيؤدي إلى فتح متصفح لإجراء المصادقة.
    * بعد الموافقة، سيتم إعادة توجيهك إلى `redirect_uri` مع رمز التفويض (`code`) في عنوان URL.
    * **انسخ** قيمة هذا الرمز (`code`) وقم بلصقها يدوياً كـ **Current Value** للمتغير **`spotify_authorization_code`** في البيئة.
2.  **Get Access Token:**
    * قم بتشغيل هذا الطلب (POST). سيستخدم رمز التفويض لتبادله برمز الوصول الفعلي (Access Token) ورمز التحديث (Refresh Token).
    * **ملاحظة:** يجب أن يحتوي على نص برمجي (Script) يقوم بتعيين قيم `spotify_access_token` و `spotify_refresh_token` تلقائيًا من الاستجابة (إذا كان هذا النص البرمجي مُفعلاً).
3.  **Get User Id:**
    * قم بتشغيل هذا الطلب. سيحصل على معرف المستخدم.
    * **مهم:** يجب أن يحتوي على نص برمجي يقوم بتعيين المتغير **`spotify_user_id`** من حقل `id` في الاستجابة.
4.  **Get Playlists / Create Playlist / Update Playlist:**
    * الآن يمكنك اختبار جميع طلبات إدارة قوائم التشغيل.

---

## 📢 مسودة منشور LinkedIn
